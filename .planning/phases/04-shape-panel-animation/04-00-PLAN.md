---
phase: "04-shape-panel-animation"
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - vitest.setup.ts
  - src/engine/drawShape.test.ts
  - src/components/HsvSliders.test.tsx
  - src/components/ShapeTypeSelector.test.tsx
  - src/store/shapeStore.test.ts
  - src/components/CellPanel.test.tsx
  - src/engine/audioEngine.test.ts
autonomous: true
requirements:
  - PANL-01
  - PANL-02
  - PANL-03
  - ANIM-01

must_haves:
  truths:
    - "All seven test files exist and can be discovered by vitest"
    - "vitest.setup.ts canvas mock includes roundRect so drawShape square-type tests do not throw"
    - "drawShape.test.ts has at least one test per shape type (circle, square, triangle, diamond, star, blob)"
    - "HsvSliders.test.tsx has stub tests for slider rendering and onChange"
    - "ShapeTypeSelector.test.tsx has stub tests for button rendering and onClick"
    - "shapeStore.test.ts has stubs for updateShape, size default, and animRate default"
    - "CellPanel.test.tsx stubs cover Phase 4 occupied-mode controls"
    - "audioEngine.test.ts stubs cover updateVoiceColor and updateVoiceSize"
  artifacts:
    - path: "vitest.setup.ts"
      provides: "canvas mock with roundRect"
      contains: "roundRect"
    - path: "src/engine/drawShape.test.ts"
      provides: "drawShape unit test scaffold"
    - path: "src/components/HsvSliders.test.tsx"
      provides: "HsvSliders unit test scaffold"
    - path: "src/components/ShapeTypeSelector.test.tsx"
      provides: "ShapeTypeSelector unit test scaffold"
  key_links:
    - from: "vitest.setup.ts"
      to: "src/engine/drawShape.ts"
      via: "roundRect mock in HTMLCanvasElement.prototype.getContext"
      pattern: "roundRect"
---

<objective>
Wave 0: Test infrastructure setup — all test scaffolds and mock extensions required by subsequent plans.

Purpose: Establish the Nyquist test harness before any implementation. Every production task in Waves 1–3 has a pre-existing test file it can run against. This prevents the "write tests after" anti-pattern and ensures vitest roundRect mock is in place before drawShape is implemented.

Output:
- `vitest.setup.ts` extended with `roundRect: () => {}`
- `src/engine/drawShape.test.ts` — drawShape scaffold (6 shape types, no-throw + pulseScale formula)
- `src/components/HsvSliders.test.tsx` — HsvSliders scaffold (rendering, onChange)
- `src/components/ShapeTypeSelector.test.tsx` — ShapeTypeSelector scaffold (rendering, onClick)
- `src/store/shapeStore.test.ts` — extended with updateShape, size/animRate defaults
- `src/components/CellPanel.test.tsx` — extended with Phase 4 occupied-mode control stubs
- `src/engine/audioEngine.test.ts` — extended with updateVoiceColor/updateVoiceSize stubs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-shape-panel-animation/04-CONTEXT.md
@.planning/phases/04-shape-panel-animation/04-RESEARCH.md
@.planning/phases/04-shape-panel-animation/04-VALIDATION.md
</context>

<interfaces>
<!-- Key types that test scaffolds must import. Will be created in Wave 1 but referenced here. -->
<!-- Tests that import non-existent files must use vi.mock or be structured as "will fail until Wave 1" -->

From src/store/shapeStore.ts (existing):
```typescript
export interface ShapeColor { h: number; s: number; l: number }
export type ShapeType = 'circle' | 'triangle' | 'square' | 'diamond' | 'star' | 'blob'
export interface Shape { id: string; col: number; row: number; color: ShapeColor; type: ShapeType }
// Wave 1 adds: size: number; animRate: number
// Wave 1 adds: updateShape(id: string, patch: Partial<Shape>): void
export const shapeStore: StoreApi<ShapeState>
export const useShapeStore: <T>(selector: (s: ShapeState) => T) => T
```

From src/engine/drawShape.ts (NEW in Wave 1 — not yet created):
```typescript
// Will be: src/engine/drawShape.ts
export function drawShape(
  ctx: CanvasRenderingContext2D,
  cx: number, cy: number, radius: number,
  type: ShapeType, color: ShapeColor
): void
```

From src/engine/audioEngine.ts (NEW exports in Wave 2 — not yet created):
```typescript
// Will be added:
export function updateVoiceColor(shapeId: string, color: ShapeColor): void
export function updateVoiceSize(shapeId: string, size: number): void
```

From src/components/HsvSliders.tsx (NEW in Wave 3 — not yet created):
```typescript
interface HsvSlidersProps { color: ShapeColor; onChange: (color: ShapeColor) => void }
export function HsvSliders(props: HsvSlidersProps): JSX.Element
```

From src/components/ShapeTypeSelector.tsx (NEW in Wave 3 — not yet created):
```typescript
interface ShapeTypeSelectorProps {
  currentType: ShapeType
  shapeColor: ShapeColor
  onChange: (type: ShapeType) => void
}
export function ShapeTypeSelector(props: ShapeTypeSelectorProps): JSX.Element
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend vitest.setup.ts and create drawShape + store test scaffolds</name>
  <files>
    vitest.setup.ts
    src/engine/drawShape.test.ts
    src/store/shapeStore.test.ts
  </files>
  <read_first>
    - vitest.setup.ts (MUST read — will extend existing mock, not replace it)
    - src/store/shapeStore.test.ts (MUST read — will add new describe blocks, not replace)
    - src/store/shapeStore.ts (read — understand current Shape interface before adding stubs)
    - .planning/phases/04-shape-panel-animation/04-RESEARCH.md (read — VALIDATION section and Wave 0 gaps list)
    - .planning/phases/04-shape-panel-animation/04-VALIDATION.md (read — per-task verification map)
  </read_first>
  <behavior>
    vitest.setup.ts:
    - Add `roundRect: () => {}` to the canvas mock object returned by HTMLCanvasElement.prototype.getContext
    - Must NOT remove any existing mock methods (fillRect, arc, beginPath, etc.)

    src/engine/drawShape.test.ts (NEW FILE — test scaffold for Wave 1 production code):
    - Import drawShape from '../engine/drawShape' (will fail until Wave 1 creates the file — tests will be red until then, which is correct)
    - Test: drawShape with type 'circle' does not throw when called on a mock ctx
    - Test: drawShape with type 'square' does not throw (requires roundRect in mock)
    - Test: drawShape with type 'triangle' does not throw
    - Test: drawShape with type 'diamond' does not throw
    - Test: drawShape with type 'star' does not throw
    - Test: drawShape with type 'blob' does not throw
    - Test: pulseScale formula `1 + 0.4 * Math.sin(2 * Math.PI * rate * t)` is in range [0.6, 1.4] for rate=1.0, any t
    - Use the vitest.setup.ts canvas mock (no real DOM needed — create a mock ctx object inline with all required methods)

    src/store/shapeStore.test.ts (EXTEND — add new describe block at bottom):
    - Add describe block: 'shapeStore — updateShape (Phase 4)'
    - Test: addShape creates shape with size=50 by default
    - Test: addShape creates shape with animRate=1.0 by default
    - Test: updateShape patches color on existing shape
    - Test: updateShape patches size on existing shape
    - Test: updateShape patches animRate on existing shape
    - Test: updateShape is a no-op when id not found
    - These tests will be RED until Wave 1 adds size/animRate/updateShape
  </behavior>
  <action>
    1. Read vitest.setup.ts first. Find the canvas mock object (the large object literal returned by getContext). Add `roundRect: () => {},` after `arc: () => {},`.

    2. Create src/engine/drawShape.test.ts as a NEW file. Structure:
    ```typescript
    // src/engine/drawShape.test.ts
    // Wave 0 scaffold — tests will be RED until Wave 1 creates src/engine/drawShape.ts
    import { describe, it, expect } from 'vitest'

    // drawShape is a pure canvas function — use a mock ctx object
    const makeMockCtx = () => ({
      beginPath: () => {}, arc: () => {}, moveTo: () => {}, lineTo: () => {},
      closePath: () => {}, fill: () => {}, stroke: () => {}, roundRect: () => {},
      fillStyle: '' as CanvasRenderingContext2D['fillStyle'],
      strokeStyle: '' as CanvasRenderingContext2D['strokeStyle'],
      lineWidth: 1,
    }) as unknown as CanvasRenderingContext2D

    describe('drawShape', () => {
      let drawShape: typeof import('../engine/drawShape').drawShape

      beforeAll(async () => {
        // Dynamic import so tests report "cannot find module" rather than crashing
        try {
          const mod = await import('../engine/drawShape')
          drawShape = mod.drawShape
        } catch {
          // File not created yet — tests will skip
        }
      })

      // Use describe.skipIf pattern — tests gracefully skip until Wave 1 creates the file
      const types = ['circle', 'square', 'triangle', 'diamond', 'star', 'blob'] as const
      const color = { h: 220, s: 70, l: 30 }

      for (const type of types) {
        it(`does not throw for type "${type}"`, () => {
          if (!drawShape) return // Skip until file exists
          const ctx = makeMockCtx()
          expect(() => drawShape(ctx, 16, 16, 10, type, color)).not.toThrow()
        })
      }
    })

    describe('pulseScale formula', () => {
      it('oscillates between 0.6 and 1.4 for any time value', () => {
        // CONTEXT.md D-12: pulseScale = 1 + 0.4 * sin(2π * animRate * t)
        const rate = 1.0
        // Test across one full cycle: t = 0, 0.25, 0.5, 0.75 seconds
        const testTimes = [0, 0.25, 0.5, 0.75, 1.0, 2.3, 7.8]
        for (const t of testTimes) {
          const pulseScale = 1 + 0.4 * Math.sin(2 * Math.PI * rate * t)
          expect(pulseScale).toBeGreaterThanOrEqual(0.59)
          expect(pulseScale).toBeLessThanOrEqual(1.41)
        }
      })

      it('returns 1.0 when animRate is 0', () => {
        const t = 999  // any time
        const pulseScale = 1 + 0.4 * Math.sin(2 * Math.PI * 0 * t)
        expect(pulseScale).toBeCloseTo(1.0, 5)
      })
    })
    ```

    3. Read src/store/shapeStore.test.ts. Append a new describe block at the very end of the file (after the closing `})` of the last existing describe block):
    ```typescript
    describe('shapeStore — Phase 4 fields and updateShape', () => {
      beforeEach(() => {
        shapeStore.setState({ shapes: [] })
        shapeStore.temporal.getState().clear()
      })

      it('addShape creates shape with size=50 by default', () => {
        shapeStore.getState().addShape(0, 0)
        const shape = shapeStore.getState().shapes[0]
        expect(shape?.size).toBe(50)
      })

      it('addShape creates shape with animRate=1.0 by default', () => {
        shapeStore.getState().addShape(0, 0)
        const shape = shapeStore.getState().shapes[0]
        expect(shape?.animRate).toBe(1.0)
      })

      it('updateShape patches color on existing shape', () => {
        shapeStore.getState().addShape(0, 0)
        const id = shapeStore.getState().shapes[0]!.id
        shapeStore.getState().updateShape(id, { color: { h: 180, s: 50, l: 60 } })
        const shape = shapeStore.getState().shapes[0]
        expect(shape?.color).toEqual({ h: 180, s: 50, l: 60 })
      })

      it('updateShape patches size on existing shape', () => {
        shapeStore.getState().addShape(0, 0)
        const id = shapeStore.getState().shapes[0]!.id
        shapeStore.getState().updateShape(id, { size: 75 })
        expect(shapeStore.getState().shapes[0]?.size).toBe(75)
      })

      it('updateShape patches animRate on existing shape', () => {
        shapeStore.getState().addShape(0, 0)
        const id = shapeStore.getState().shapes[0]!.id
        shapeStore.getState().updateShape(id, { animRate: 5.0 })
        expect(shapeStore.getState().shapes[0]?.animRate).toBe(5.0)
      })

      it('updateShape is a no-op when id not found', () => {
        shapeStore.getState().addShape(0, 0)
        const before = shapeStore.getState().shapes[0]?.color
        shapeStore.getState().updateShape('non-existent-id', { size: 99 })
        expect(shapeStore.getState().shapes[0]?.color).toEqual(before)
        expect(shapeStore.getState().shapes).toHaveLength(1)
      })
    })
    ```
  </action>
  <verify>
    <automated>cd C:/src/GitHub/sound-image && npx vitest run vitest.setup.ts src/store/shapeStore.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - vitest.setup.ts contains the string `roundRect: () => {}`
    - src/engine/drawShape.test.ts exists and contains `describe('drawShape'`
    - src/engine/drawShape.test.ts contains `describe('pulseScale formula'`
    - src/store/shapeStore.test.ts contains `describe('shapeStore — Phase 4 fields and updateShape'`
    - src/store/shapeStore.test.ts contains `updateShape(id, { color:`
    - `npx vitest run src/store/shapeStore.test.ts` exits 0 (existing tests pass; new tests will be red until Wave 1 — that is expected and OK)
    - The pulseScale formula tests in drawShape.test.ts pass immediately (they are pure math, no import dependency)
  </acceptance_criteria>
  <done>vitest.setup.ts has roundRect mock; drawShape.test.ts scaffold exists with 6-type no-throw tests + pulseScale formula tests; shapeStore.test.ts has Phase 4 describe block with updateShape/size/animRate tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create HsvSliders, ShapeTypeSelector, CellPanel, and audioEngine test scaffolds</name>
  <files>
    src/components/HsvSliders.test.tsx
    src/components/ShapeTypeSelector.test.tsx
    src/components/CellPanel.test.tsx
    src/engine/audioEngine.test.ts
  </files>
  <read_first>
    - src/components/CellPanel.test.tsx (MUST read — will extend, not replace)
    - src/engine/audioEngine.test.ts (MUST read — will extend, not replace)
    - .planning/phases/04-shape-panel-animation/04-UI-SPEC.md (read — component inventory and interaction contract)
    - .planning/phases/04-shape-panel-animation/04-CONTEXT.md (read — D-01 through D-07 for slider and selector specs)
    - .planning/phases/04-shape-panel-animation/04-RESEARCH.md (read — VALIDATION section Wave 0 gaps)
  </read_first>
  <behavior>
    src/components/HsvSliders.test.tsx (NEW FILE):
    - Test: renders three range inputs (hue 0-360, saturation 0-100, lightness 0-100)
    - Test: renders label text 'Hue', 'Saturation', 'Lightness'
    - Test: onChange fires with updated h when hue slider changes
    - Test: onChange fires with updated s when saturation slider changes
    - Test: onChange fires with updated l when lightness slider changes
    - Will be RED until Wave 3 creates HsvSliders.tsx

    src/components/ShapeTypeSelector.test.tsx (NEW FILE):
    - Test: renders 6 buttons (one per shape type)
    - Test: each button has aria-label "{type} shape"
    - Test: active type button has aria-pressed="true"
    - Test: clicking a type button calls onChange with that type
    - Will be RED until Wave 3 creates ShapeTypeSelector.tsx

    src/components/CellPanel.test.tsx (EXTEND — add new describe block at bottom):
    - Keep ALL existing tests unchanged
    - The existing test 'renders occupied-cell mode' checks for 'Type', 'circle', 'Hue', 'Remove Shape'
    - These labels will CHANGE in Wave 3 (CellPanel occupied mode is replaced) — mark the old tests with a comment
    - Add new describe block 'CellPanel — Phase 4 occupied mode' with stubs that will pass after Wave 3:
      - Test: renders Hue slider in occupied mode
      - Test: renders Saturation slider in occupied mode
      - Test: renders Lightness slider in occupied mode
      - Test: renders Size slider in occupied mode
      - Test: renders Rate slider in occupied mode
      - Test: renders 6 shape type buttons in occupied mode
      - Test: renders Remove Shape button in occupied mode

    src/engine/audioEngine.test.ts (EXTEND — add new describe blocks at bottom):
    - Keep ALL existing tests unchanged
    - Add describe block 'updateVoiceColor (Phase 4)':
      - Test: updateVoiceColor returns undefined when no voice exists for shapeId (no-op, no throw)
      - Test: updateVoiceColor returns undefined when AudioContext unavailable (jsdom)
    - Add describe block 'updateVoiceSize (Phase 4)':
      - Test: updateVoiceSize returns undefined when no voice exists for shapeId (no-op, no throw)
      - Test: updateVoiceSize returns undefined when AudioContext unavailable (jsdom)
  </behavior>
  <action>
    1. Create src/components/HsvSliders.test.tsx as a NEW file:
    ```typescript
    // src/components/HsvSliders.test.tsx
    // Wave 0 scaffold — tests RED until Wave 3 creates HsvSliders.tsx
    import { describe, it, expect, vi } from 'vitest'
    import { render, screen, fireEvent } from '@testing-library/react'
    import type { ShapeColor } from '../store/shapeStore'

    // Dynamic import to avoid crash when file does not exist yet
    let HsvSliders: React.ComponentType<{ color: ShapeColor; onChange: (c: ShapeColor) => void }> | null = null
    beforeAll(async () => {
      try {
        const mod = await import('./HsvSliders')
        HsvSliders = mod.HsvSliders
      } catch { /* not created yet */ }
    })

    const defaultColor: ShapeColor = { h: 220, s: 70, l: 30 }

    describe('HsvSliders', () => {
      it('renders Hue, Saturation, Lightness labels', () => {
        if (!HsvSliders) return
        render(<HsvSliders color={defaultColor} onChange={() => {}} />)
        expect(screen.getByText('Hue')).toBeTruthy()
        expect(screen.getByText('Saturation')).toBeTruthy()
        expect(screen.getByText('Lightness')).toBeTruthy()
      })

      it('renders three range inputs', () => {
        if (!HsvSliders) return
        const { container } = render(<HsvSliders color={defaultColor} onChange={() => {}} />)
        const inputs = container.querySelectorAll('input[type="range"]')
        expect(inputs.length).toBe(3)
      })

      it('hue range input has min=0 and max=360', () => {
        if (!HsvSliders) return
        render(<HsvSliders color={defaultColor} onChange={() => {}} />)
        const hueInput = screen.getByLabelText(/Hue, 0 to 360/i)
        expect(hueInput.getAttribute('min')).toBe('0')
        expect(hueInput.getAttribute('max')).toBe('360')
      })

      it('saturation range input has min=0 and max=100', () => {
        if (!HsvSliders) return
        render(<HsvSliders color={defaultColor} onChange={() => {}} />)
        const satInput = screen.getByLabelText(/Saturation, 0 to 100/i)
        expect(satInput.getAttribute('min')).toBe('0')
        expect(satInput.getAttribute('max')).toBe('100')
      })

      it('onChange fires with updated h when hue slider changes', () => {
        if (!HsvSliders) return
        const onChange = vi.fn()
        render(<HsvSliders color={defaultColor} onChange={onChange} />)
        const hueInput = screen.getByLabelText(/Hue, 0 to 360/i)
        fireEvent.change(hueInput, { target: { value: '180' } })
        expect(onChange).toHaveBeenCalledWith({ h: 180, s: 70, l: 30 })
      })

      it('onChange fires with updated s when saturation slider changes', () => {
        if (!HsvSliders) return
        const onChange = vi.fn()
        render(<HsvSliders color={defaultColor} onChange={onChange} />)
        const satInput = screen.getByLabelText(/Saturation, 0 to 100/i)
        fireEvent.change(satInput, { target: { value: '50' } })
        expect(onChange).toHaveBeenCalledWith({ h: 220, s: 50, l: 30 })
      })

      it('onChange fires with updated l when lightness slider changes', () => {
        if (!HsvSliders) return
        const onChange = vi.fn()
        render(<HsvSliders color={defaultColor} onChange={onChange} />)
        const lightInput = screen.getByLabelText(/Lightness, 0 to 100/i)
        fireEvent.change(lightInput, { target: { value: '60' } })
        expect(onChange).toHaveBeenCalledWith({ h: 220, s: 70, l: 60 })
      })
    })
    ```

    2. Create src/components/ShapeTypeSelector.test.tsx as a NEW file:
    ```typescript
    // src/components/ShapeTypeSelector.test.tsx
    // Wave 0 scaffold — tests RED until Wave 3 creates ShapeTypeSelector.tsx
    import { describe, it, expect, vi, beforeAll } from 'vitest'
    import { render, screen, fireEvent } from '@testing-library/react'
    import type { ShapeType, ShapeColor } from '../store/shapeStore'

    let ShapeTypeSelector: React.ComponentType<{
      currentType: ShapeType
      shapeColor: ShapeColor
      onChange: (type: ShapeType) => void
    }> | null = null

    beforeAll(async () => {
      try {
        const mod = await import('./ShapeTypeSelector')
        ShapeTypeSelector = mod.ShapeTypeSelector
      } catch { /* not created yet */ }
    })

    const ALL_TYPES: ShapeType[] = ['circle', 'triangle', 'square', 'diamond', 'star', 'blob']
    const defaultColor: ShapeColor = { h: 220, s: 70, l: 30 }

    describe('ShapeTypeSelector', () => {
      it('renders 6 type buttons', () => {
        if (!ShapeTypeSelector) return
        render(<ShapeTypeSelector currentType="circle" shapeColor={defaultColor} onChange={() => {}} />)
        const buttons = screen.getAllByRole('button')
        expect(buttons.length).toBeGreaterThanOrEqual(6)
      })

      it('each button has aria-label "{type} shape"', () => {
        if (!ShapeTypeSelector) return
        render(<ShapeTypeSelector currentType="circle" shapeColor={defaultColor} onChange={() => {}} />)
        for (const type of ALL_TYPES) {
          expect(screen.getByLabelText(`${type} shape`)).toBeTruthy()
        }
      })

      it('active type button has aria-pressed="true"', () => {
        if (!ShapeTypeSelector) return
        render(<ShapeTypeSelector currentType="triangle" shapeColor={defaultColor} onChange={() => {}} />)
        const activeBtn = screen.getByLabelText('triangle shape')
        expect(activeBtn.getAttribute('aria-pressed')).toBe('true')
      })

      it('non-active type buttons have aria-pressed="false"', () => {
        if (!ShapeTypeSelector) return
        render(<ShapeTypeSelector currentType="circle" shapeColor={defaultColor} onChange={() => {}} />)
        const triangleBtn = screen.getByLabelText('triangle shape')
        expect(triangleBtn.getAttribute('aria-pressed')).toBe('false')
      })

      it('clicking a type button calls onChange with that type', () => {
        if (!ShapeTypeSelector) return
        const onChange = vi.fn()
        render(<ShapeTypeSelector currentType="circle" shapeColor={defaultColor} onChange={onChange} />)
        fireEvent.click(screen.getByLabelText('triangle shape'))
        expect(onChange).toHaveBeenCalledWith('triangle')
      })
    })
    ```

    3. Read src/components/CellPanel.test.tsx. Append at the bottom (after closing `})` of last describe):
    ```typescript
    // Phase 4 occupied mode controls — RED until Wave 3 replaces CellPanel occupied mode
    describe('CellPanel — Phase 4 occupied mode controls', () => {
      beforeEach(() => {
        selectionStore.setState({ selectedCell: null })
        shapeStore.setState({ shapes: [] })
      })

      it('renders Hue slider in occupied mode', () => {
        shapeStore.getState().addShape(0, 0)
        selectionStore.setState({ selectedCell: { col: 0, row: 0 } })
        render(<CellPanel />)
        // After Wave 3: HsvSliders renders an input with aria-label "Hue, 0 to 360"
        expect(screen.queryByLabelText(/Hue, 0 to 360/i)).toBeTruthy()
      })

      it('renders Size slider in occupied mode', () => {
        shapeStore.getState().addShape(0, 0)
        selectionStore.setState({ selectedCell: { col: 0, row: 0 } })
        render(<CellPanel />)
        expect(screen.queryByLabelText(/Size, 0 to 100/i)).toBeTruthy()
      })

      it('renders animation rate slider in occupied mode', () => {
        shapeStore.getState().addShape(0, 0)
        selectionStore.setState({ selectedCell: { col: 0, row: 0 } })
        render(<CellPanel />)
        expect(screen.queryByLabelText(/Animation rate/i)).toBeTruthy()
      })

      it('renders 6 shape type buttons in occupied mode', () => {
        shapeStore.getState().addShape(0, 0)
        selectionStore.setState({ selectedCell: { col: 0, row: 0 } })
        const { container } = render(<CellPanel />)
        // After Wave 3: ShapeTypeSelector renders 6 buttons with aria-label "{type} shape"
        const typeButtons = container.querySelectorAll('[aria-label$=" shape"]')
        expect(typeButtons.length).toBeGreaterThanOrEqual(6)
      })

      it('Remove Shape button still present in Phase 4 occupied mode', () => {
        shapeStore.getState().addShape(0, 0)
        selectionStore.setState({ selectedCell: { col: 0, row: 0 } })
        render(<CellPanel />)
        expect(screen.getByText('Remove Shape')).toBeTruthy()
      })
    })
    ```

    4. Read src/engine/audioEngine.test.ts. Append at the bottom (after last describe block):
    ```typescript
    // Phase 4 — updateVoiceColor and updateVoiceSize
    // These functions guard against missing voice / missing AudioContext (jsdom)
    describe('updateVoiceColor (Phase 4)', () => {
      it('returns undefined (no-op) when AudioContext unavailable in jsdom', async () => {
        // In jsdom, AudioContext is not available — the function must guard and return
        const { updateVoiceColor } = await import('./audioEngine')
        expect(() => updateVoiceColor('nonexistent', { h: 120, s: 50, l: 50 })).not.toThrow()
      })
    })

    describe('updateVoiceSize (Phase 4)', () => {
      it('returns undefined (no-op) when AudioContext unavailable in jsdom', async () => {
        const { updateVoiceSize } = await import('./audioEngine')
        expect(() => updateVoiceSize('nonexistent', 75)).not.toThrow()
      })
    })
    ```
  </action>
  <verify>
    <automated>cd C:/src/GitHub/sound-image && npx vitest run src/components/HsvSliders.test.tsx src/components/ShapeTypeSelector.test.tsx --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/HsvSliders.test.tsx exists and contains `describe('HsvSliders'`
    - src/components/HsvSliders.test.tsx contains `getByLabelText(/Hue, 0 to 360/i)`
    - src/components/ShapeTypeSelector.test.tsx exists and contains `describe('ShapeTypeSelector'`
    - src/components/ShapeTypeSelector.test.tsx contains `aria-pressed`
    - src/components/CellPanel.test.tsx contains `describe('CellPanel — Phase 4 occupied mode controls'`
    - src/engine/audioEngine.test.ts contains `describe('updateVoiceColor (Phase 4)'`
    - src/engine/audioEngine.test.ts contains `describe('updateVoiceSize (Phase 4)'`
    - `npx vitest run src/engine/audioEngine.test.ts` exits 0 (existing tests pass; new stubs also pass since they test no-op behavior)
    - `npx vitest run src/engine/drawShape.test.ts` — pulseScale formula tests pass; drawShape tests skip/fail gracefully (no crash)
  </acceptance_criteria>
  <done>All 7 test scaffold files exist. vitest.setup.ts has roundRect. Pure math tests (pulseScale) pass. Mocked-file tests skip gracefully. audioEngine no-op tests pass. All existing tests in audioEngine.test.ts and CellPanel.test.tsx continue to pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test harness → production code | Test scaffold files import production modules; no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00-01 | Elevation | vitest.setup.ts mock replacement | accept | Wave 0 only modifies test infrastructure; mock changes are scoped to test environment only, no production code path |

No high-severity threats identified. Wave 0 is test infrastructure only — no user input, no DOM rendering, no audio, no network.
</threat_model>

<verification>
Run the full test suite after Wave 0 completes:

```bash
cd C:/src/GitHub/sound-image && npx vitest run --reporter=verbose
```

Expected: all pre-existing tests pass (shapeStore, audioEngine pure functions, CellPanel existing tests, canvasEngine). New scaffold tests either pass (pure math tests, audioEngine no-op stubs) or fail gracefully with import errors (not TypeScript compile errors).

Check file existence:
```bash
ls C:/src/GitHub/sound-image/src/engine/drawShape.test.ts
ls C:/src/GitHub/sound-image/src/components/HsvSliders.test.tsx
ls C:/src/GitHub/sound-image/src/components/ShapeTypeSelector.test.tsx
```
</verification>

<success_criteria>
- vitest.setup.ts canvas mock includes `roundRect: () => {}`
- All 7 test scaffold/extension files exist
- Full vitest suite exits with 0 failures on pre-existing tests
- pulseScale formula tests in drawShape.test.ts pass (pure math, no dependency)
- audioEngine Phase 4 no-op stub tests pass (guard against missing AudioContext)
</success_criteria>

<output>
After completion, create `.planning/phases/04-shape-panel-animation/04-00-SUMMARY.md`
</output>
