// Wave 0 stub — implementations added in Plan 03
// Covers: CANV-01 cell math
import { describe, it } from 'vitest'

describe('canvasEngine cell math', () => {
  it.todo('cellAtPoint returns correct col/row for center of each cell')
  it.todo('cellAtPoint returns null for clicks outside the grid area')
  it.todo('cellAtPoint clamps to [0,3] for edge clicks')
})
