// Wave 0 stub — implementations added in Plan 02
// Covers: CANV-01 store behavior
import { describe, it, beforeEach } from 'vitest'

describe('shapeStore', () => {
  beforeEach(() => {
    // Store reset handled per-test in Plan 02
  })

  it.todo('addShape adds a shape to an empty cell')
  it.todo('addShape ignores placement on an occupied cell')
  it.todo('shapes array has correct col/row after addShape')
})
