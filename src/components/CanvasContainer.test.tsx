// Wave 0 stub — implementations added in Plan 04
// Covers: CANV-01 click-to-place integration
import { describe, it } from 'vitest'

describe('CanvasContainer', () => {
  it.todo('clicking an empty cell calls shapeStore.addShape with correct col/row')
  it.todo('clicking an occupied cell does not call addShape again')
})
